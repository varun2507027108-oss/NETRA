"""Optional machine-learning integrations for NETRA.

All modules in this package require optional extras (e.g. `pip install
netra-core[ml]`) and must NEVER be imported by `netra_core.__init__` or
any statutory engine modules (s1..s7). The core engine remains pure
Python standard library.
"""
